FROM node:24-alpine

WORKDIR /app/frontend

ENV CI=true

# This image is intended to be driven by /app/frontend-test.sh, which mounts the
# frontend source and reuses a named node_modules volume for dependency caching.
CMD ["sh", "-lc", "echo 'Use ./frontend-test.sh to run frontend tests in Docker.'"]
